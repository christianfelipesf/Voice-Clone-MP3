# Dublador - dependencias base (leves, instalam sem compilador)
#
# Instalacao:
#   pip install -r requirements.txt
#
# Obs: ffmpeg precisa estar instalado no sistema e no PATH (nao via pip).
#
# O script tambem instala automaticamente qualquer dependencia faltante
# quando ela e usada pela primeira vez (pip sob demanda). Este arquivo
# so garante o pacote base de uma vez.

# Audio
numpy>=1.24
soundfile>=0.12

# Interface grafica
customtkinter>=5.2
psutil>=5.9

# Painel web (dublar_web.py + pasta static/)
flask>=3.0

# Modo automatico (sem .srt): transcricao + traducao
faster-whisper>=1.1
deep-translator>=1.11

# Motor leve (sem clonagem): Edge TTS
edge-tts>=6.1

# YouTube
yt-dlp>=2025.1
